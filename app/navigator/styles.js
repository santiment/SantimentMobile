/**
 * @flow
 */

import {
    StyleSheet,
} from 'react-native';

const getStyles = (): StyleSheet => (
    StyleSheet.create({
        navigator: {
            flex: 1,
        },
    })
);

export default getStyles;
